const colors = ['red', 'blue', 'yellow'];

colors.forEach((element) => console.log(element + ' something'));

function addMr(string) {
    return 'Mr' + string;
}

colors.forEach()

function addP(word) {
    return word + ' dupa'
}

addL = word => word + ' leg';



const newColors = colors.map(el => el + 'head');

console.log(newColors);




// let string = 'Czerwona';



// console.log(addP(string));

