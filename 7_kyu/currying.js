let ex = [1,2,3];

function multiplyAll(arrayTo) {


    return arrayTo = (2)
}


//solutions from Codewars

function multiplyAll(arr) {
    return function(n) {
      return arr.map(x => x * n);
    }
  }

//

  const multiplyAll = arr => n => arr.map(x => n * x);